try:
    m = int(input("Enter value of m: "))
    n = int(input("Enter value of n: "))

    result = m / n
    print("Result =", result)

except ZeroDivisionError:
    print("Error: Cannot divide by zero!")

# Factorial function
def factorial(n):
    fact = 1
    for i in range(1, n+1):
        fact *= i
    return fact

# Power function
def power(n):
    return n * n

# List of vowels
vowels = ['a', 'e', 'i', 'o', 'u']
✔️ Step 2: Main Program
import variables

n = int(input("Enter number: "))

# Factorial
print("Factorial =", variables.factorial(n))

# Power
print("Power =", variables.power(n))

# Second vowel
print("Second vowel =", variables.vowels[1])    
